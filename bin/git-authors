#!/usr/bin/env bash

FILES=$(git diff master HEAD --name-only)

git shortlog \
  -sne --no-merges \
  $(git log --since 2015-01-01 --until 2015-01-31 --reverse | head -n 1 | awk '{print $2}')..HEAD \
  "$@"

echo $FILES
